{
    "name": "magiskepiksler.com",
    "version": "1.0.0",
    "description": "",
    "main": "dist/index.html",
    "scripts": {
        "build": "rm -rf dist && tinyjam src dist && cp -R cloudflare/. dist/ && node bin/versjoner.mjs dist",
        "start": "npm run build && wrangler dev",
        "deploy": "npm run build && wrangler deploy"
    },
    "author": "Magnus Haugsand",
    "engines": {
        "node": ">=22"
    },
    "devDependencies": {
        "tinyjam": "^0.4.0",
        "wrangler": "^4.131.0"
    }
}
